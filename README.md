# Lab W3D5: vLLM Benchmark & Capacity Triage

---




## 📋 Overview
This repository contains the benchmarking and capacity evaluation artifacts for **W3D5**, executed on a Google Colab **T4 GPU** environment running **vLLM** with `Qwen/Qwen2.5-1.5B-Instruct`.

---

## 📊 Benchmark Results Summary
The automated sweep tested multiple concurrency levels against 20 fixed prompt inputs (`prompts.txt`), tracking token throughput, time-to-first-token (TTFT), and p95 latency.

* **Concurrency 1 & 2:** High token throughput and stable latency as the T4 GPU efficiently processes the requests.
* **Concurrency 4 (The Knee Point):** Performance shifts significantly, marking the critical operational threshold and the system's knee point.
* **Concurrency 8 & 16:** Reaches the absolute hardware capacity limits and VRAM saturation of the T4 GPU.

---

## 🔍 Capacity Analysis & Knee Point
* **Determined Knee Concurrency:** `4`
* **Target P95 SLO:** `3.0` seconds
* **Limiting Factor:** **Memory / VRAM Saturation**. Higher concurrency levels (8 and 16) reach the hardware operational limits of the T4 GPU.

---

## 📁 Repository Artifacts
* `bench_report.json`: Full raw metrics and structured run history generated by `bench.py`.
* `knee.json`: Saved configuration containing the target SLO and computed optimal knee concurrency.
* `capacity-note.md`: Summary triage note detailing the limiting factors and sustainable request rates.
* `verify.py`: Automated verification script ensuring workspace compliance.

---

## 🛠️ Verification
To run the automated validation locally or via CI/CD:
```bash
python verify.py

```

Expected Output:

Plaintext
```text   
GREEN CHECK: PASS
```

<img width="1194" height="132" alt="Screenshot 2026-09-03 150633" src="https://github.com/user-attachments/assets/f844e7fe-f149-417b-9764-68eafdf1f046" />
